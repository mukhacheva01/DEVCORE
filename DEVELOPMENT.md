# 📱 DevCore Landing Page - Документация по разработке

## 🎯 Обзор проекта

Современный лендинг для DevCore, созданный в стиле креативного агентства [Setters Agency](https://www.setters.agency/). Проект представляет собой одностраничный сайт с современным дизайном, анимациями и интерактивными элементами.

---

## 🏗️ Структура проекта

```
DEVCORE/
├── index.html          # Основная HTML-разметка
├── styles.css          # Стили и анимации
├── script.js           # JavaScript функциональность
├── README.md           # Описание проекта
└── DEVELOPMENT.md      # Документация разработки (этот файл)
```

---

## 🎨 Дизайн-система

### Цветовая палитра

```css
--color-bg: #0a0a0a               /* Основной фон */
--color-text: #ffffff              /* Основной текст */
--color-text-secondary: #999999    /* Вторичный текст */
--color-accent: #6366f1            /* Акцентный цвет (индиго) */
--color-accent-secondary: #8b5cf6  /* Вторичный акцент (фиолетовый) */
--color-border: #1a1a1a            /* Границы */
--color-card-bg: #111111           /* Фон карточек */
```

### Типографика

- **Основной шрифт**: SF Pro Display, системные шрифты (Apple/Windows)
- **Заголовки**: 40px - 120px (адаптивные)
- **Основной текст**: 16px - 18px
- **Мелкий текст**: 13px - 14px

### Эффекты и анимации

1. **Градиенты**
   - Цветные градиенты для акцентов
   - Анимированные градиенты на заголовках
   - Плавные переходы цветов

2. **Transitions**
   - Smooth: `cubic-bezier(0.16, 1, 0.3, 1)` - 600ms
   - Fast: `cubic-bezier(0.16, 1, 0.3, 1)` - 300ms

3. **Hover эффекты**
   - Подъем карточек на 3-5px
   - Изменение цвета границ
   - Появление градиентных линий

---

## 📄 Структура HTML

### Основные секции

1. **Навигация** (`<nav class="navbar">`)
   - Фиксированная шапка с blur-эффектом
   - Адаптивное меню для мобильных устройств
   - Плавная прокрутка к секциям

2. **Героический блок** (`<section class="hero">`)
   - Крупный заголовок с анимациями
   - Индикатор прокрутки
   - Фоновые эффекты

3. **Услуги** (`<section class="services">`)
   - Теги технологий
   - Call-to-action кнопка
   - Центрированное содержимое

4. **Проекты** (`<section class="projects">`)
   - Карточки кейсов
   - Hover-эффекты
   - Ссылки на детали

5. **О нас** (`<section class="about">`)
   - Описание компании
   - Статистика (анимированные числа)

6. **Контакты** (`<section class="contact">`)
   - Контактная информация
   - Форма обратной связи
   - Валидация полей

7. **Футер** (`<footer class="footer">`)
   - Социальные сети
   - Копирайт
   - Дополнительные ссылки

---

## 🎭 CSS-архитектура

### Методология

Используется **компонентный подход** с организацией по секциям:

```css
/* 1. Переменные */
:root { ... }

/* 2. Базовые стили */
*, html, body { ... }

/* 3. Компоненты */
.navbar { ... }
.hero { ... }
.projects { ... }

/* 4. Утилиты */
.fade-in { ... }
.gradient-text { ... }

/* 5. Адаптивность */
@media (max-width: 768px) { ... }
```

### Ключевые классы

- `.gradient-text` - Анимированный градиентный текст
- `.bracket` - Стилизованные скобки `(( ))`
- `.fade-in` - Анимация появления при прокрутке
- `.project-card` - Карточка проекта с hover-эффектами
- `.cta-button` - Основная кнопка призыва к действию

### Адаптивность

Использованы:
- `clamp()` для адаптивной типографики
- CSS Grid с `auto-fit` для сеток
- Media queries для мобильных устройств
- Flexbox для гибких компоновок

```css
/* Пример адаптивного размера */
font-size: clamp(40px, 8vw, 120px);

/* Адаптивная сетка */
grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
```

---

## ⚙️ JavaScript функциональность

### Основные модули

#### 1. Анимации при прокрутке
```javascript
initScrollAnimations()
```
- Использует **Intersection Observer API**
- Добавляет класс `.fade-in` при попадании в viewport
- Отписывается после срабатывания

#### 2. Индикатор прокрутки
```javascript
initScrollIndicator()
```
- Показывает процент прокрутки страницы
- Обновляется в реальном времени
- Формат: `((N%))`

#### 3. Мобильное меню
```javascript
initMobileMenu()
```
- Гамбургер-меню для экранов < 768px
- Автоматическое закрытие при выборе пункта
- Плавные переходы

#### 4. Форма контактов
```javascript
initContactForm()
```
- Валидация полей
- Отправка данных (требует backend)
- Показ модального окна успеха

#### 5. Плавная прокрутка
```javascript
initSmoothScroll()
```
- Перехват кликов по якорным ссылкам
- Smooth scrolling с offset для navbar

#### 6. Параллакс эффект
```javascript
window.addEventListener('scroll', ...)
```
- Движение фона с эффектом глубины
- Transform на основе позиции прокрутки

#### 7. Анимация чисел
```javascript
animateNumbers()
```
- Постепенное увеличение чисел в статистике
- Срабатывает при скролле до секции
- Использует `requestAnimationFrame`

#### 8. 3D эффект карточек
```javascript
document.addEventListener('mousemove', ...)
```
- Наклон карточек при наведении мыши
- Эффект perspective и rotate

#### 9. Скрытие navbar
```javascript
window.addEventListener('scroll', ...)
```
- Скрывается при прокрутке вниз
- Появляется при прокрутке вверх
- Улучшает UX на мобильных устройствах

---

## 🚀 Установка и запуск

### Простой способ (без сборщиков)

1. Клонируйте проект:
```bash
cd DEVCORE
```

2. Откройте `index.html` в браузере:
   - Двойной клик на файл
   - Или через Live Server в VS Code

### С локальным сервером

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js (с установленным http-server)
npx http-server -p 8000

# PHP
php -S localhost:8000
```

Откройте: `http://localhost:8000`

---

## 🔧 Настройка и кастомизация

### Изменение цветов

Отредактируйте CSS-переменные в `styles.css`:

```css
:root {
    --color-accent: #your-color;        /* Основной акцент */
    --color-accent-secondary: #your-color; /* Дополнительный акцент */
    --color-bg: #your-color;            /* Фон */
}
```

### Добавление нового проекта

В `index.html` добавьте в секцию `.projects`:

```html
<div class="project-card">
    <div class="project-badge">
        <span class="badge-symbol">((D))</span>
    </div>
    <h3 class="project-title">Название проекта</h3>
    <p class="project-description">Описание проекта</p>
    <a href="#" class="project-link">Смотреть кейс</a>
</div>
```

### Изменение контента

1. **Заголовок**: редактируйте `.hero-title` в index.html
2. **Услуги**: добавьте/удалите `.tag` элементы
3. **Статистика**: измените числа в `.stat-number`
4. **Контакты**: обновите email адреса в `.contact-card`

---

## 📱 Адаптивность

### Брейкпоинты

```css
/* Мобильные устройства */
@media (max-width: 768px) { ... }

/* Планшеты */
@media (min-width: 769px) and (max-width: 1024px) { ... }

/* Десктоп */
@media (min-width: 1025px) { ... }
```

### Тестирование

Протестируйте на:
- iPhone (Safari)
- Android (Chrome)
- iPad
- Десктопные браузеры (Chrome, Firefox, Safari, Edge)

---

## 🎯 Производительность

### Оптимизации

1. **CSS**
   - Минимизация перерисовок
   - Использование `transform` вместо `top/left`
   - Hardware acceleration через `will-change`

2. **JavaScript**
   - Debounce для scroll events
   - Intersection Observer вместо scroll listeners
   - RequestAnimationFrame для анимаций

3. **Загрузка**
   - Критический CSS inline (опционально)
   - Отложенная загрузка изображений (при добавлении)
   - Минификация файлов

### Метрики производительности

Целевые показатели:
- **First Contentful Paint**: < 1.5s
- **Largest Contentful Paint**: < 2.5s
- **Total Blocking Time**: < 300ms
- **Cumulative Layout Shift**: < 0.1

---

## 🔌 Интеграция с backend

### API для формы

Пример интеграции с backend в `script.js`:

```javascript
form.addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(form);
    const data = Object.fromEntries(formData);
    
    try {
        const response = await fetch('/api/contact', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(data)
        });
        
        if (response.ok) {
            showModal();
            form.reset();
        } else {
            // Обработка ошибки
            alert('Произошла ошибка. Попробуйте позже.');
        }
    } catch (error) {
        console.error('Error:', error);
    }
});
```

### Рекомендуемый стек backend

- **Node.js + Express** - для простого API
- **Python + FastAPI** - для ML-интеграций
- **PHP + Laravel** - для традиционных проектов
- **Serverless** (Vercel, Netlify Functions) - для минимальной инфраструктуры

---

## 🎨 Дизайн-референсы

Проект вдохновлен стилем:
- [Setters Agency](https://www.setters.agency/) - основной референс
- [Awwwards](https://www.awwwards.com/) - современные тренды
- [Dribbble](https://dribbble.com/) - UI/UX паттерны

### Ключевые характеристики стиля

✅ Темная тема с яркими акцентами  
✅ Крупная, выразительная типографика  
✅ Минималистичный подход  
✅ Плавные анимации и переходы  
✅ Использование скобок `(( ))` как фирменный элемент  
✅ Градиенты и свечения  
✅ Пространство и "воздух" в дизайне  

---

## 🐛 Известные проблемы и решения

### Проблема: Navbar перекрывает контент при прокрутке

**Решение**: Добавлен offset в функцию `initSmoothScroll()`

```javascript
const offsetTop = target.offsetTop - 80;
```

### Проблема: Анимации тормозят на мобильных

**Решение**: Используйте `will-change` осторожно и только при необходимости:

```css
.project-card:hover {
    will-change: transform;
}
```

### Проблема: Форма не работает

**Решение**: Требуется backend. Для тестирования используйте [Formspree](https://formspree.io/) или подобные сервисы.

---

## 📦 Деплой

### Статический хостинг

#### Vercel
```bash
npm i -g vercel
vercel
```

#### Netlify
```bash
npm i -g netlify-cli
netlify deploy
```

#### GitHub Pages
1. Загрузите файлы в репозиторий
2. Settings → Pages → выберите ветку
3. Сайт будет доступен по адресу `username.github.io/repo`

#### Cloudflare Pages
1. Подключите GitHub репозиторий
2. Настройте автоматический деплой
3. Получите бесплатный CDN

---

## 🔒 SEO и Метатеги

Добавьте в `<head>`:

```html
<!-- SEO -->
<meta name="description" content="DevCore - Цифровые решения нового поколения. Разработка веб-приложений, мобильных приложений и AI-решений.">
<meta name="keywords" content="разработка, веб-разработка, мобильные приложения, UI/UX дизайн">
<meta name="author" content="DevCore">

<!-- Open Graph -->
<meta property="og:title" content="DevCore - Цифровые решения">
<meta property="og:description" content="Создаем цифровые продукты нового поколения">
<meta property="og:image" content="/preview.jpg">
<meta property="og:url" content="https://devcore.dev">

<!-- Twitter Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="DevCore">
<meta name="twitter:description" content="Цифровые решения нового поколения">
<meta name="twitter:image" content="/preview.jpg">

<!-- Favicon -->
<link rel="icon" type="image/png" href="/favicon.png">
```

---

## 📈 Аналитика

### Google Analytics

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

### Yandex Metrika

```html
<!-- Yandex.Metrika counter -->
<script type="text/javascript">
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();
   for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
   k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(XXXXXX, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true,
        webvisor:true
   });
</script>
```

---

## 🧪 Тестирование

### Checklist

- [ ] Все ссылки работают
- [ ] Форма отправляет данные
- [ ] Анимации плавные (60fps)
- [ ] Адаптивность на всех устройствах
- [ ] Кроссбраузерность (Chrome, Firefox, Safari, Edge)
- [ ] Нет ошибок в консоли
- [ ] SEO метатеги заполнены
- [ ] Favicon установлен
- [ ] Lighthouse score > 90

### Инструменты

- **Lighthouse** - производительность и SEO
- **BrowserStack** - тестирование на разных устройствах
- **WAVE** - доступность
- **PageSpeed Insights** - скорость загрузки

---

## 📚 Дополнительные ресурсы

### Библиотеки для расширения

- [GSAP](https://greensock.com/gsap/) - продвинутые анимации
- [AOS](https://michalsnik.github.io/aos/) - анимации при скролле
- [Locomotive Scroll](https://locomotivemtl.github.io/locomotive-scroll/) - плавный скролл
- [Three.js](https://threejs.org/) - 3D графика
- [Particles.js](https://vincentgarreau.com/particles.js/) - фоновые эффекты

### Полезные ссылки

- [CSS Tricks](https://css-tricks.com/)
- [MDN Web Docs](https://developer.mozilla.org/)
- [Can I Use](https://caniuse.com/)
- [Cubic Bezier Generator](https://cubic-bezier.com/)
- [Gradient Generator](https://cssgradient.io/)

---

## 👥 Команда и контрибьюторы

**Разработка**: DevCore Team  
**Дизайн**: Вдохновлен Setters Agency  
**Версия**: 1.0.0  
**Дата**: Декабрь 2025

---

## 📝 Лицензия

Проект создан для внутреннего использования DevCore. При использовании кода укажите авторство.

---

## 🎉 Начало работы

1. Откройте `index.html` в браузере
2. Изучите структуру кода
3. Настройте цвета и контент под свой бренд
4. Интегрируйте backend для формы
5. Добавьте реальные кейсы и проекты
6. Оптимизируйте изображения
7. Настройте аналитику
8. Задеплойте на хостинг

**Удачи в разработке! 🚀**

---

*Документация обновлена: 22.12.2025*




